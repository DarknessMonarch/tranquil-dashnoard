export const metadata = {
  title: "Admin Login",
  description: "Sign in to Tranquil Property Management Admin Portal",
};

export default function AdminLoginLayout({ children }) {
  return children;
}
