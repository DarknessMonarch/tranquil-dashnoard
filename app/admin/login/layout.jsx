export const metadata = {
  title: "Admin Login",
  description: "Sign in to Rental Property Management Admin Portal",
};

export default function AdminLoginLayout({ children }) {
  return children;
}
